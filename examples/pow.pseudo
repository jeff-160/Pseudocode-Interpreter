// manual power computation

BEGIN
    PROCEDURE square(a: INTEGER, b: INTEGER)
        DECLARE res : INTEGER
        res <- 1

        FOR i <- 0 TO b - 1
            res <- res * a
        ENDFOR

        OUTPUT res
    ENDPROCEDURE

    CALL square(2, 3)
    CALL square(10, 5)
END